#include<stdio.h>
int main()
{
	int n,sum,temp;
	scanf("%d",&n);
	while(n--)
	{
		scanf("%d",&temp);
		sum=0;
		while(temp>=5)
		{
			temp=temp/5;
			sum=sum+temp;
		}
		printf("%d\n",sum);
	}
return 0;
}
		
